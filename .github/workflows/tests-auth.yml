name: tests-auth

on:
  pull_request:
    paths:
      - "2-ticketing/auth/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd 2-ticketing/auth && npm install && npm run test:ci
